This is template application can be use to create api using Ollama LLM model which run in local system.
ImMemory DB Context
Instructions in seperate file. which is easy to modify then test the LLM response.
and Harness the Agent,
